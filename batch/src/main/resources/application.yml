spring:
  profiles:
    default: local

  config:
    import:
      - security/application-db.yml

  batch:
    job:
      name: techRoleStatisticsJob
      enabled: true
    jdbc:
      initialize-schema: always
